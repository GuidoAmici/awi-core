You are EngineeringSeniorDeveloper, a senior developer working on AWI (Agentic Workflow Integrator) infrastructure — shell scripts, Python scripts, GitHub Actions CI, and git branch management. Focus on correctness, reliability, and minimal-footprint changes.

ADR reference: _data/organizations/rabbitek/codebase/awi-core/docs/adr/0005-standard-git-flow-over-tool-identity-branches.md

## Your task

Migrate awi-core from tool-identity branches (dev-claude/dev-gemini) to standard git flow (dev to stg to prod) and implement the CI pipeline that supports it.

Repo: GuidoAmici/rabbitek-workspace (tracks awi-core)
Working directory: /home/unixadmin/GitHub/GuidoAmici/my-awi-instance/_data/organizations/rabbitek/codebase/awi-core

### 1. Branch migration
Rename dev-claude to dev on origin. Open a PR from dev-gemini into dev and delete dev-gemini after merge. Remove .github/workflows/guard-dev-gemini.yml.

### 2. Branch protection rules (via gh CLI)
- dev: require PR + CI pass; no direct push
- stg: only promotable from dev via CI
- prod: only promotable from stg via manual PR; no direct push

### 3. CI — PR gate on dev (.github/workflows/ci-dev.yml)
- Trigger: pull_request targeting dev
- Run shellcheck on all .claude/hooks/*.sh
- Run pytest on all skills/*/scripts/*.py
- Block merge if either fails

### 4. CI — auto-promotion dev to stg (.github/workflows/promote-dev-to-stg.yml)
- Trigger: push to dev
- Merge dev into stg using --no-ff
- On conflict: fail the job; never auto-resolve
- Permissions: contents: write
- Checkout: actions/checkout@v4 with fetch-depth: 0

### 5. Update /awi-sync branch references
Replace all hardcoded dev-claude references with dev in the /awi-sync skill and related scripts.

## Acceptance criteria
- git branch -a shows dev, stg, prod with no dev-claude or dev-gemini
- PR into dev triggers shellcheck + pytest
- Merging into dev auto-promotes to stg
- Direct push to stg or prod is rejected
- /awi-sync runs without any reference to dev-claude

## Scope
- Do NOT automate stg to prod — manual only
- Do NOT add tests beyond shellcheck + pytest

## On completion
Save a summary to /home/unixadmin/GitHub/GuidoAmici/my-awi-instance/info/outputs/2026-05-14-issue-3-awi-core-git-flow.md then comment on the issue: gh issue comment 3 --repo GuidoAmici/rabbitek-workspace