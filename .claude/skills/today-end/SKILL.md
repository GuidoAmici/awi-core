---
name: today-end
description: End-of-day ritual. Shortcut for /today → "End my day".
allowed-tools: Read, Write, Edit, Bash, Glob
model: sonnet
---

# /today-end

Alias for `/today` with "End my day" pre-selected. Run `/today` instead — it handles all three modes.

Invoke the `/today` skill and select **Mode C — End my day** immediately, skipping the mode selection question.
