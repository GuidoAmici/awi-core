---
name: today-start
description: Morning intake ritual. Shortcut for /today → "Start my day".
---

# /today-start

Alias for `/today` with "Start my day" pre-selected. Run `/today` instead — it handles all three modes.

Invoke the `/today` skill and select **Mode A — Start my day** immediately, skipping the mode selection question.
